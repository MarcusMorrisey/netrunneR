Continue executing the netrunneR offline-mirror-plan build on mediaServer. This is a resume of a long execution session — read this whole brief before doing anything, then act (no need to re-plan or re-derive what's below).

## Access
- mediaServer via `ssh marcus@mediaserver` (key-based, works from this Windows host already).
- GitHub repo: `https://github.com/MarcusMorrisey/netrunneR` (public — was deliberately flipped from private because the Dockerfile's anonymous `git clone` needs it public; do not revert to private).
- Repo checked out at `~/src/netrunneR` on mediaServer (this IS the working copy — edit here, commit, push).
- homelab infra repo at `~/src/homelab` on mediaServer; netrunner-mirror deployment files live at `~/src/homelab/compose/netrunner-mirror/`.
- Real `.env` (git-ignored, already created) at `~/src/homelab/compose/netrunner-mirror/.env` with real values including `NETRUNNER_PKG_SHA` — **update this SHA whenever you push a new netrunneR commit that changes anything Docker-relevant**, via `cd ~/src/netrunneR && git rev-parse HEAD`.
- Authoritative spec: `docs/netrunneR/offline-mirror-plan.md` in the homelab repo (561 lines, finalized). Also `docs/netrunneR/netrunneR-implementation-plan.md` — the fully rendered, QR-approved implementation plan (6,372 lines) — useful for cross-referencing original design intent, but you are now well past that plan in some areas (see "Real API corrections" below) since it was never validated against real APIs until this session.

## Status: what's actually verified working right now
- `cd ~/src/netrunneR && make test` → **FAIL 0 | WARN 0 | SKIP 2 | PASS 83**
- `cd ~/src/netrunneR && make check` → **0 errors, 0 warnings, 1 note** (note is `dbplyr`/`lubridate`/`readr`/`stringr` declared-but-unused Imports — deliberately left, not gamed, doesn't block `error_on="warning"`)
- Docker image builds successfully: `cd ~/src/homelab/compose/netrunner-mirror && export LINEAGE=cardpool MODE=backfill && docker compose build sync` (works, ~663MB image, takes ~60-90s)
- Container actually runs and reaches real lineage dispatch logic (proven by hitting the cardpool `repo_url` gap below, not a build/infra failure)
- 5 store directories exist and are correctly owned: `/srv/{abr,nrdb,cardpool,rules,implementation}-data`, `marcus:marcus`, `0700`
- M-009 (notifier) files are staged (uncommitted) in `~/src/homelab`: `git status --short` there shows `compose/netrunner-mirror/` (untracked), `docs/netrunneR/notifier.md`, `docs/netrunneR/rollout-checklist.md`, plus the pre-existing modified `docs/netrunneR/offline-mirror-plan.md` (that one is NOT yours to touch — leave it, it's this session's earlier finalized-spec edit).

## Real defects found and fixed this session (all committed to netrunneR, 17+ commits on master)
Full list is in the git log, but the categories: hunk-header/backslash-escaping artifacts from the original diff generation; Makefile never restored deps; renv bootstrap needed GitHub install not CRAN (pinned `v1.2.4`); renv.lock had to be regenerated from a real working install (`type="all"` snapshot, not `type="explicit"` — explicit misses Suggests); missing system libs (curl binary, full graphics/font stack, libuv); `build_revision()`'s `pkg-src` dependency broke outside Docker — fixed with a real `find_package_root()`/`pkg_src_dir` fallback that now works under `devtools::test()`, `R CMD check`, AND Docker; `apply_schema()` broke on SQL comments containing semicolons; `fetch_abr()`/`run_decklist_sweep()` crashed on legitimately-empty API results (zero-column tibble bug — same root pattern, fixed twice); `abr_get()`'s custom 5xx handling was dead code because httr2 auto-errors before it runs; a recursive test mock caused a C-stack overflow that silently hung the whole suite; `roxygen2::roxygenise()` had literally never been run (man/ didn't exist); missing LICENSE file; invalid Rd name for `%||%`; missing `@param` docs; Makefile excluded from build tarball (GNU-extension portability warning); `.ci/check.R` now creates `inst/pkg-src` ephemerally (git-ignored) before `R CMD check` so it exercises the same code path Docker does.

**Do not re-litigate any of the above** — they're done and verified. If `make test`/`make check` regress, something you touched broke them; check your own diff first.

## THE ACTUAL NEXT WORK: real upstream API/page structures don't match what the code assumes
None of this was caught by planning-phase QR because nothing was ever actually run against real endpoints until this session. This is real engineering work, not polish.

### 1. cardpool + implementation lineages (LOW RISK — just wire in known-good URLs)
Real sources (user-confirmed):
- cardpool: `https://github.com/Null-Signal-Games/netrunner-cards-json.git`
- implementation: `https://github.com/mtgred/netrunner.git`

`R/lineage.R`'s `.LINEAGE_REGISTRY` and `lineage()` currently never set `repo_url` for cardpool/implementation at all (only test fixtures ever set it) — this is why the container run failed with `is.character(url) is not TRUE` inside `gert::git_clone()`. Add `repo_url` to both registry entries and thread it through `lineage()`'s `do.call(new_lineage, ...)` call (currently only passes `schedule`, `schema_version`, `pacing`, `build_module_path` — needs `repo_url` and, for rules, `hub_url` too, see below). Also check whether a `ref` (branch) needs setting — `fetch-git-mirror.R` defaults to `"main"` if `lineage$ref` is NULL; verify both repos actually use `main` as default branch (don't assume, check with `git ls-remote --symref <url> HEAD`).

### 2. ABR lineage (MEDIUM-HIGH RISK — real endpoint/field mismatch, user already said "go ahead and fix it")
Real API, confirmed via `alwaysberunning.net/apidoc`:
- Base URL: `https://alwaysberunning.net/api`
- `/tournaments/results?limit=500` (supports `offset` too) — matches current code path, keep.
- `/tournaments/upcoming` — matches, keep.
- **`/entries?id=<tournament_id>`** — query param. Current code calls `/tournaments/%s/entries` (path-based). Wrong, must fix in `fetch-abr.R`'s `entries <- purrr::map(tournaments$id, function(id) abr_get(..., sprintf("/tournaments/%s/entries", id)))` → should be `abr_get(lineage$base_url, "/entries", list(id = id))`.
- **`/videos`** — ONE bulk call returning ALL tournaments' videos "organized by tournament", not per-tournament. Current code calls `/tournaments/%s/videos` once per tournament ID in a `purrr::map` loop. Wrong — must become a single `abr_get(lineage$base_url, "/videos")` call, then the result structure needs inspecting (haven't seen a live response yet — do a real `curl https://alwaysberunning.net/api/videos` or similar to see actual shape before writing the parse logic).
- **Tournament object real fields**: `id, title, date, type, format, location, location_country, location_state, address, store, cardpool, players_count, top_count, concluded, winner_runner_identity, winner_corp_identity`. Current `ABR_TOURNAMENT_ALLOWLIST` in `R/build-abr.R` and the `tournament` table in `inst/sql/schema/abr.sql` assume WRONG fields: `id, name, date, format, region, country, player_count, cut_size, identity_a_code, identity_c_code`. These need remapping to the real field names. **Important**: the doc summary listed zero personal-data/email fields on the tournament object — before assuming the two-layer personal-data defense (`ABR_DENY_PATTERN`/`check_deny_pattern()`) is now moot, actually fetch a real `/tournaments/results` response and inspect every field the API returns (not just the summary's paraphrase) to confirm nothing personal leaks through. Don't remove the deny-pattern defense without that real check — it may still be needed for the `/entries` endpoint's player fields (`user_id, user_name, user_import_name` — `user_name`/`user_import_name` could plausibly be real names).
- After remapping, `build-abr.R`, `abr.sql`, and every ABR test fixture (`test-abr-allowlist.R`, `test-abr-backfill.R`, `test-capture-boundary.R`'s ABR-touching test, `test-fetch-api-poll.R`) need updating to match. Re-run `make test` after.

### 3. NRDB lineage (MEDIUM RISK — real API shape is simpler than assumed, needs a small redesign not a tweak)
Real API, confirmed via `netrunnerdb.com/api/doc` and live `curl`:
- Base URL: `https://netrunnerdb.com/api/2.0/public`
- `/reviews`, `/rulings` paths match current code, but **the real response envelope is `{"data": [...]}`** — a plain array of item objects, wrapped only in `data`. NO `total`, `last_updated`, or `version_number` fields exist at the top level (confirmed via `curl https://netrunnerdb.com/api/2.0/public/reviews`). Current `fetch_nrdb()` in `R/fetch-nrdb.R` reads `reviews$total`, `reviews$last_updated`, `reviews$version_number` directly and feeds them into `compare_field()` against the previous release's manifest — none of that data exists in the real response. This whole comparison mechanism needs redesigning: options are (a) compare `length(reviews$data)` as a row-count proxy instead of a `total` field, (b) drop the `last_updated`/`version_number` comparisons entirely since there's no real analog, or (c) something else — **use your judgment, but don't invent fake fields to paper over this; either find a real substitute or honestly drop the check**. Re-verify against a live response for `/rulings` too before assuming its shape matches `/reviews`.
- Decklist-by-date: real path is `/decklists/by_date/{date}` — **date is a path segment** (with underscore `by_date`, not hyphen `by-date`), not a query parameter. Current `run_decklist_sweep()` in `R/decklist-sweep.R` calls `nrdb_get(lineage$base_url, "/decklists/by-date", list(date = ...))` (query param, wrong separator). Fix: `nrdb_get(lineage$base_url, sprintf("/decklists/by_date/%s", format(d, "%Y-%m-%d")))`. Response envelope is presumably also `{"data": [...]}` — verify with a live curl before assuming `page$results` (current code) is right; it's almost certainly wrong too, same as reviews/rulings.

### 4. Rules lineage (HIGH RISK — scraper was invented against a page structure that doesn't exist, full rewrite needed)
Real page: `https://nullsignal.games/rules/comp-rules/` ("Comprehensive Rules Hub"). Confirmed via browser inspection: **zero `<table>` elements** on the page. Current `parse_rules_hub_index()` in `R/fetch-web-archive.R` selects `table.rules-index tr` with CSS classes `.version`/`.published-date`/`.title`/`a.pdf-link` — none of this exists. Real structure (already inspected):
- Latest version is in a Kadence "info-box" block (`div.wp-block-kadence-infobox`), containing a link like `<a href=".../Null_Signal_Games_Netrunner_Comprehensive_Rules_v26.03.pdf">`, plus a sibling "_Annotated" PDF variant (exclude the annotated one) and a "(Web)" non-PDF link (exclude, not a PDF).
- Older/archival versions are plain `<p><a href="...vXX.XX....pdf">Netrunner Comprehensive Rules vXX.XX </a></p>` links further down the page, including even-older `NISEI Comprehensive Rules v1.X` naming (different prefix, same `vX.X` version pattern).
- There's a SEPARATE "Card Text Updates" section further down with its own PDF links — must be excluded (not rules documents).
- **No published date is visible anywhere in the page text.** Real, working substitute already confirmed: each PDF's HTTP response has a genuine `Last-Modified` header (verified via `curl -sI` on the v26.03 PDF: `last-modified: Mon, 02 Mar 2026 00:18:05 GMT`). Fetch each candidate PDF with a HEAD request and use that header for `published_date`, rather than inventing a date from page order or scraping absent text.
- Suggested approach: select `a[href$=".pdf"]` within `.entry-content`, filter to link text/href containing "Comprehensive_Rules" or "Comprehensive Rules" (case-insensitive) and NOT containing "Annotated" or "Card-Text"/"Card Text", extract version via a regex tolerant of both `v26.03`-style and `v1.6`-style and `v.25.04`-style (note the inconsistent dot-after-v in some historical entries — check real hrefs/text, don't assume one pattern), then issue `httr2::req_perform()` with `method = "HEAD"` per candidate PDF to pull `Last-Modified` for `published_date`. Rewrite `pool_pdf()`/`fetch_lineage.netrunneR_web_archive()` as needed to match this actually-buildable tibble shape.
- `lineage()`/`.LINEAGE_REGISTRY` also needs a `hub_url` field wired in for rules (currently missing, same gap as `repo_url` for the git-mirror lineages) — set it to `https://nullsignal.games/rules/comp-rules/`.

## Working method established this session (keep doing this)
- Edit files locally in this session's scratchpad, or directly via heredoc/`scp` to mediaServer — whichever is faster; the source of truth is the git checkout at `~/src/netrunneR` on mediaServer.
- After any code change: `cd ~/src/netrunneR && git add -A && git commit -m "..." && git push origin master`, then re-run `make test` (and periodically `make check`) directly on mediaServer via SSH before moving on. Don't batch up many unverified changes.
- `make test`/`make check`/`make document` all take 60-120s (mostly package install time) — run them via background/long-timeout Bash calls, not synchronously assumed-fast.
- GitHub API calls (e.g. `remotes::install_github`) have occasionally hit transient 504s/connectivity blips during this session — if a step fails with a GitHub-related network error, just retry once or twice before assuming it's a real bug.
- After `R/lineage.R` or any `R/*.R` change, remember `make document` if roxygen comments changed (regenerates `man/` + `NAMESPACE`), then commit those generated files too.
- Once ABR/NRDB/rules-hub lineages are fixed and `make test`/`make check` pass again, the real next milestone is: commit M-009 files already staged in `~/src/homelab`, then actually test-run each of the 5 lineages for real (`docker compose run --rm sync` with `LINEAGE=<name> MODE=backfill` for each), confirm real data lands correctly in `/srv/<name>-data`, THEN move to M-011 (installing the 10 systemd unit files + `netrunner-mirror-failure@.service`, but per the locked plan **do not enable any timer** until a disposable test failure has been sent through the real notifier and a human — the user — has confirmed receipt; `NOTIFIER_TOPIC_URL` in `.env` is currently a placeholder `https://ntfy.sh/netrunner-mirror-marcus`, confirm that's really what they want before wiring it live).

## Guardrails to keep observing
- Never guess a "close enough" URL, field name, or response shape for an external API — verify with a real `curl`/`WebFetch`/browser check before writing code against it. This session's entire remaining-work list exists because that rule was skipped during original planning.
- Don't relitigate visibility (repo stays public) or the Makefile/renv/pkg-src fixes above.
- `.env` on mediaServer has real values including an NRDB contact email and notifier topic — don't overwrite it carelessly; check current contents before regenerating.
- Store dirs (`/srv/*-data`) needed a human to create via `sudo` — you (the new session) also won't have passwordless sudo; if you hit another root-requiring step, ask the user to run it rather than trying to work around it.
