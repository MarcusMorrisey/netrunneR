% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_matchup_explorer.R
\name{matchup_display}
\alias{matchup_display}
\title{Shared matchup display pipeline}
\usage{
matchup_display(matchup, codes, cards)
}
\arguments{
\item{matchup}{A matchup tibble as returned by \code{\link[=compute_ice_breaker_matchups]{compute_ice_breaker_matchups()}}.}

\item{codes}{Optional character vector of permitted codes; \code{NULL} permits all.}

\item{cards}{The card table supplying titles.}
}
\value{
The matchup rows filtered to the code set, with \code{ice_title} and \code{breaker_title}
joined by \code{code}, ordered by \code{cost_to_break} with \code{na.last = TRUE}.
}
\description{
Shared matchup display pipeline
}
\details{
Extracted so the explorer and Deck Compare share the pipeline and not the
table. The reactable itself is deliberately not shared: the explorer collapses to one
Counterpart column only because its subject is constant on every row, while Deck
Compare varies on both axes, so a shared component would need a mode flag - the very
selector this module's doc comment removed. (DL-036)
}
\keyword{internal}
