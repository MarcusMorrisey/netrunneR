% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deck-compare.R
\name{deck_matchups}
\alias{deck_matchups}
\title{Pair a Corp deck's resolved ice against a Runner deck's resolved breakers}
\usage{
deck_matchups(matchup, corp_ice_codes, runner_breaker_codes, corp_copy_counts)
}
\arguments{
\item{matchup}{A matchup tibble as returned by \code{compute_ice_breaker_matchups()}.}

\item{corp_ice_codes}{Character vector of the Corp deck's resolved ice codes.}

\item{runner_breaker_codes}{Character vector of the Runner deck's resolved breaker
codes.}

\item{corp_copy_counts}{Named integer vector of card code to quantity, as carried
on the Corp deck's \code{cards}.}
}
\value{
The rows of the precomputed matchup tibble whose \code{ice_code} is in the Corp
deck's resolved ice and whose \code{breaker_code} is in the Runner deck's resolved
breakers, with the column set of \code{\link[=compute_ice_breaker_matchups]{compute_ice_breaker_matchups()}} unchanged, plus a
separate named integer vector of ice copy counts keyed by code.
}
\description{
Pair a Corp deck's resolved ice against a Runner deck's resolved breakers
}
\details{
Nothing is recomputed: \code{cost_to_break}, \code{credit_differential} and \code{source}
arrive as compute_ice_breaker_matchups() left them, \code{not_computable} included.
Copy counts are returned alongside rather than as a column because both values are
per-title properties invariant to how many copies a deck runs; a copy column would
be a field the single-card modal must carry and ignore. (DL-038, DL-032)
}
\keyword{internal}
