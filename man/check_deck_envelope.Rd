% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fetch-deck.R
\name{check_deck_envelope}
\alias{check_deck_envelope}
\title{Validate a decklist response envelope before any field is read}
\usage{
check_deck_envelope(envelope, id)
}
\description{
Validate a decklist response envelope before any field is read
}
\details{
Validates the decklist envelope as \code{success} identical to \code{TRUE}, \code{total}
identical to \code{1}, and \code{data} present and non-empty, before any deck field is read.
This is deliberately NOT \code{\link[=compare_shape]{compare_shape()}}, which asserts only that a bare \code{data}
list is present because that is the real shape of /reviews and /rulings. Applied to
the decklist endpoint, compare_shape() would accept an error envelope carrying
\code{success: false} alongside a \code{data} key. (DL-034)
}
\keyword{internal}
