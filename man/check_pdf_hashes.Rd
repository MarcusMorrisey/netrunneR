% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-rules.R
\name{check_pdf_hashes}
\alias{check_pdf_hashes}
\title{Require every recorded pdf_url's current bytes to match its stored hash}
\usage{
check_pdf_hashes(index, raw_dir)
}
\arguments{
\item{index}{A tibble with \code{version} and \code{pooled_hash} columns.}

\item{raw_dir}{Character. The staged raw directory holding \verb{objects/}.}
}
\value{
A check-result list with \code{check}, \code{status}, and \code{message}.
}
\description{
A mismatch blocks promotion and leaves the pool object untouched rather
than overwriting it, so a corrupted or tampered download can never
silently replace a previously verified object in the content-addressed
pool.
}
\keyword{internal}
