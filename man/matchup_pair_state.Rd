% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_lane_board.R
\name{matchup_pair_state}
\alias{matchup_pair_state}
\title{What the strip under one breaker is actually saying}
\usage{
matchup_pair_state(
  ice,
  breaker,
  pair,
  traits = NULL,
  overridden = character(0)
)
}
\arguments{
\item{ice, breaker}{One-row card frames.}

\item{pair}{That pair's matchup row, or a zero-row frame.}

\item{traits}{\code{ice_breaker_traits}, or NULL. NULL collapses the two
absent-row cases back into one, rather than guessing which applies.}

\item{overridden}{Character vector of "\if{html}{\out{<ice>}}|\if{html}{\out{<breaker>}}" keys.}
}
\value{
A list with \code{kind} ("known", "cannot_break", "assumed",
"unknown"), \code{pair} (a matchup-shaped row, possibly zero-row), \code{key}
and \code{overridable}.
}
\description{
compute_ice_breaker_matchups() emits a row only for SUBTYPE-COMPATIBLE
pairs, so on this board -- where the operator stacks whatever cards
they like -- an absent row is the common case and it has two entirely
different meanings:
}
\details{
\itemize{
\item the breaker declares what it breaks and this ice is not that, so the
filter dropped the pair ON PURPOSE. That is knowledge, and the
strongest kind: a Fracter cannot break a Code Gate, ever. Saying
"not computable" here understates what we know to nothing.
\item the breaker's break clause could not be read at all, so we do not
know what it breaks. That really is "we cannot tell you".
}

This module used to render both as \code{not_computable}, on the reasoning
that an absent row and an uncomputable one are equally "we cannot tell
you". That holds only while nothing can distinguish them; \code{traits} can.

The first case is OVERRIDABLE, because subtype is not immutable during
a game -- effects add subtypes to ice, and an AI breaker or a one-off
can break something its printed clause does not name. The override is
an assertion by the operator about the board in front of them, so the
arithmetic it unlocks is badged \code{assumed} rather than \code{formula}: the
numbers are real, the premise is theirs.
}
\keyword{internal}
