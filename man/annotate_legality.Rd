% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legality.R
\name{annotate_legality}
\alias{annotate_legality}
\title{Annotate cards with rotation and ban-list status}
\usage{
annotate_legality(
  cards,
  packs,
  rotation_cycle,
  mwl_card,
  rotation = NULL,
  mwl = NULL
)
}
\arguments{
\item{cards}{The cardpool \code{card} table.}

\item{packs}{The cardpool \code{pack} table (supplies each card's cycle).}

\item{rotation_cycle}{The cardpool \code{rotation_cycle} table.}

\item{mwl_card}{The cardpool \code{mwl_card} table.}

\item{rotation}{A one-row rotation from \code{\link[=active_rotation]{active_rotation()}}, or NULL.}

\item{mwl}{A one-row ban list from \code{\link[=active_mwl]{active_mwl()}}, or NULL.}
}
\value{
\code{cards} with the four columns above added.
}
\description{
Adds four columns rather than filtering, so a UI can show a banned
card greyed out (and say why) instead of having it silently vanish --
the same reasoning behind \code{matchup.source} carrying
\code{"not_computable"} rather than dropping the row.
}
\details{
\itemize{
\item \code{in_rotation}  -- FALSE if the card's pack belongs to a rotated-out
cycle. TRUE for every card when no rotation is supplied.
\item \code{is_banned}    -- TRUE when the active list sets \code{deck_limit} to 0.
\item \code{is_restricted} -- TRUE when the active list flags the card restricted.
\item \code{influence_penalty} -- \code{universal_faction_cost} where the list sets
one, otherwise 0.
}
}
