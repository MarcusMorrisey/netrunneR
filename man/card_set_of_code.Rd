% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legality.R
\name{card_set_of_code}
\alias{card_set_of_code}
\title{Map card codes to their v2 card set}
\usage{
card_set_of_code(codes, pack_codes, printing, card_set)
}
\arguments{
\item{codes}{Character vector of card codes.}

\item{pack_codes}{Character vector of the same length: each card's own
\code{pack_code}. Taken from the cards themselves rather than looked up
from a pack table, which is keyed by pack and cannot answer "which
pack is this CARD in".}

\item{printing}{The cardpool \code{printing} table.}

\item{card_set}{The cardpool \code{card_set} table.}
}
\value{
Character vector of card set ids, NA where unresolvable.
}
\description{
Uses the \code{printing} table, falling back to the card's own \code{pack_code}
resolved through \code{card_set$legacy_code} for any code with no printing
row. The two agree 1:1 upstream, so the fallback matters only for a
mirrored commit predating the v2 tree or a hand-built fixture -- but
it is what keeps a card from silently dropping out of every pool.
}
\keyword{internal}
