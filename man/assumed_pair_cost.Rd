% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_lane_board.R
\name{assumed_pair_cost}
\alias{assumed_pair_cost}
\title{Price a pair the subtype filter excluded, on the operator's say-so}
\usage{
assumed_pair_cost(ice, breaker, traits)
}
\arguments{
\item{ice, breaker}{One-row card frames.}

\item{traits}{\code{ice_breaker_traits}.}
}
\value{
A one-row matchup-shaped tibble.
}
\description{
Runs exactly the same arithmetic compute_ice_breaker_matchups() would
have, for the one pair it declined to emit. Nothing here reaches into
the matchup table or writes to it: the assumption belongs to one
session and one lane, and baking it into the shared table would make
one operator's board state everybody's data.
}
\details{
\code{source} comes back "assumed", never "formula". The subtraction is the
same either way; what differs is that a person supplied the premise,
and the badge has to keep saying so.
}
\keyword{internal}
