% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legality.R
\name{active_rotation}
\alias{active_rotation}
\title{The rotation in force on a given date}
\usage{
active_rotation(rotation, as_of = Sys.Date())
}
\arguments{
\item{rotation}{The cardpool \code{rotation} table.}

\item{as_of}{Date. Defaults to today.}
}
\value{
A one-row data frame, or NULL.
}
\description{
Upstream ships every rotation ever applied; the active one is simply
the most recent whose \code{date_start} has passed. Returns NULL when the
table is empty or every rotation is still in the future, which callers
must treat as "no rotation applies" rather than "nothing is legal".
}
