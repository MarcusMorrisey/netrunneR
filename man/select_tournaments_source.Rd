% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{select_tournaments_source}
\alias{select_tournaments_source}
\title{Choose which tournament frame \code{load_ice_breaker_app_data()} serves}
\usage{
select_tournaments_source(merged_release, abr_result)
}
\arguments{
\item{merged_release}{The \code{read_active_release_tables("cobra", ...)}
result (or \code{NULL}).}

\item{abr_result}{The \code{query_active_release("abr", ...)} result (or
\code{NULL}).}
}
\value{
The merged tournament frame when available, else the abr
frame when available, else \code{NULL}. (DL-057)
}
\description{
Extracted from \code{load_ice_breaker_app_data()} as its own function so
the fallback choice itself -- not just its effect after a full store
fixture load -- has a direct unit test (test-operations.R) alongside
the fixture-driven integration coverage in test-store-fixture.R.
}
\details{
Both queries are always run (rather than short-circuiting to skip the
abr query when cobra's merged read succeeds): \code{load_ice_breaker_app_data()}
runs once per process, not per request, so the cost of an unused extra
query is negligible next to the benefit of a selection function with
no side effects of its own to reason about.
}
\keyword{internal}
