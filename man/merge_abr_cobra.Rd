% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge-abr-cobra.R
\name{merge_abr_cobra}
\alias{merge_abr_cobra}
\title{Merge abr and cobra tournament records into one abr-shaped feed}
\usage{
merge_abr_cobra(
  abr_tournament,
  cobra_tournament,
  cobra_stage,
  cobra_standing,
  cardpool_titles,
  verified_matches = read_abr_cobra_verified_matches(),
  verified_deletes = read_abr_cobra_verified_deletes(),
  verified_groups = read_abr_cobra_verified_group_matches()
)
}
\arguments{
\item{abr_tournament}{A data frame shaped like \code{abr.tournament}
(id, title, date, format, type, location_state, location_country,
location_lat, location_lng, players_count, top_count,
winner_runner_identity, winner_corp_identity).}

\item{cobra_tournament}{A data frame shaped like \code{cobra.tournament}
(must include at least tournament_id, name, date).}

\item{cobra_stage}{A data frame shaped like \code{cobra.stage} (must include
tournament_id, player_count).}

\item{cobra_standing}{A data frame shaped like \code{cobra.standing} (must
include tournament_id, stage_format, rounds_complete, position,
corp_identity, runner_identity).}

\item{cardpool_titles}{A data frame with \code{code}/\code{title} columns, as
returned by \code{cardpool_card_titles()}.}

\item{verified_matches, verified_deletes, verified_groups}{Override
readers, injectable for tests; default to the packaged CSVs via
\code{R/abr-cobra-xref.R}.}
}
\value{
A list with \code{tournament_merged} and \code{tournament_merged_source}.
}
\description{
Applies the human-verified override log
(\code{read_abr_cobra_verified_deletes()}, \code{read_abr_cobra_verified_group_matches()},
\code{read_abr_cobra_verified_matches()} -- R/abr-cobra-xref.R) before the
algorithmic tier runs, exactly the precedence those readers' own
docstrings require: deletes remove a cobra row from consideration
entirely, group matches union several cobra rows into one abr-recorded
event, verified 1:1 matches are forced, and only the ids no earlier
tier claimed reach the algorithmic tier. Anything left over on either
side becomes its own single-source row. (DL-059)
}
\details{
Returns two frames rather than one: \code{tournament_merged} keeps
\code{abr.tournament}'s exact column set (so the app's read path is a
straight \code{query_active_release()} swap with no reshape), and
\code{tournament_merged_source} is a one-to-many provenance table, because a
group match can have several contributing cobra rows under one merged
id -- a flat \code{abr_id}/\code{cobra_tournament_id} column pair on the first
table cannot represent that. (DL-049, DL-050)
}
\keyword{internal}
