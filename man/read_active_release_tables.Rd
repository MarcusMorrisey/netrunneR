% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{read_active_release_tables}
\alias{read_active_release_tables}
\title{Read whole tables from a lineage's active release in one connection}
\usage{
read_active_release_tables(lineage_name, db_filename, tables)
}
\arguments{
\item{lineage_name}{Character. One of \code{BUILTIN_LINEAGES}.}

\item{db_filename}{Character. SQLite filename under \code{processed_dir}.}

\item{tables}{Character vector of table names to read.}
}
\value{
A list with \code{release} and \code{tables} (named, NULL per absent
table), or NULL if there is no active release or no database file.
}
\description{
The multi-table counterpart to \code{\link[=query_active_release]{query_active_release()}}: the card
browser needs the card table plus six legality tables from the same
cardpool database, and opening one connection per table would resolve
\code{active} repeatedly, which \code{\link[=resolve_release]{resolve_release()}} exists specifically to
avoid (a promote() landing mid-read would otherwise be observed as two
different releases).
}
\details{
A table absent from the database yields NULL rather than an error. A
release promoted before the format/card-pool schema landed genuinely
has no \code{format_snapshot} table, and the app must degrade to "no
legality data" instead of failing to start -- that state persists
until the next sync rebuilds the lineage.
}
\keyword{internal}
