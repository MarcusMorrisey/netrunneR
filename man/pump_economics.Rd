% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-implementation.R
\name{pump_economics}
\alias{pump_economics}
\title{Read a \verb{(strength-pump ...)} cost, whatever form it takes}
\usage{
pump_economics(body)
}
\arguments{
\item{body}{Character. One defcard body.}
}
\value{
A list with \code{pump_cost} (credits), \code{pump_amount} (strength
gained), \code{pump_stealth} (how many of those credits must be stealth,
NA if none need be), \code{pump_resource_type}/\code{pump_resource_qty} (a
non-credit cost, NA if there is none) and \code{has_pump}.
}
\description{
The pump clause has four shapes in the source, and only the first was
ever read:
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{(strength-pump 2 2)                             plain credits
(strength-pump (->c :credit 1 \{:stealth 1\}) 3)  credits, stealth-sourced
(strength-pump [(->c :power 1)] 2)              a non-credit resource
(strength-pump [(->c :virus 1) 3] 2)            both at once (Hantu)
}\if{html}{\out{</div>}}

A regex bounded to two integers matches only the first, so the other
three produced no pump at all -- and compute_cost_to_break_formula()
reads "no pump" as "this breaker cannot reach ice above its own
strength", which is a DEFINITE WRONG ANSWER rather than a missing one.
Eleven breakers were affected, every stealth breaker among them
(Refractor, Switchblade, Dagger, Blackstone, Houdini, Penrose,
Afterimage) plus Audrey v2, Faust, Hantu and Propeller. That is the
failure this whole table exists to avoid: an honest NA says "we do not
know", and these were saying "it cannot be done".

STEALTH CREDITS ARE STILL CREDITS. \verb{\{:stealth 1\}} means one of the
credits must come from a stealth card, and \verb{:all-stealth} means all of
them must; neither changes how many credits are paid. So the count
goes in \code{pump_cost} as normal and the sourcing constraint is recorded
separately in \code{pump_stealth}, rather than inventing a second currency.
Power and virus counters and trashing a card are NOT credits, and are
kept out of \code{pump_cost} entirely for the same reason -- adding them in
would be a conversion rate nobody has defined.
}
\keyword{internal}
