% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{query_active_release}
\alias{query_active_release}
\title{Resolve a lineage's active release and run one query against its
processed SQLite database}
\usage{
query_active_release(lineage_name, db_filename, sql)
}
\arguments{
\item{lineage_name}{Character. One of \code{BUILTIN_LINEAGES}.}

\item{db_filename}{Character. SQLite filename under the release's
\code{processed_dir} (e.g. \code{"cardpool.sqlite"}).}

\item{sql}{Character. Query to run.}
}
\value{
A list with \code{release} (the \code{resolve_release()} result) and
\code{data} (the query result), or \code{NULL} if there's no active release or
the db file is missing.
}
\description{
Generalizes the connect/query/disconnect sequence
\code{cardpool_codes()} (\code{R/build-implementation.R}) and the ice/breaker
Shiny app both need -- factored out here rather than duplicated across
three call sites (cardpool_codes(), and two near-identical blocks that
used to live directly in app_server.R for cardpool and implementation).
}
\keyword{internal}
