% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge-abr-cobra.R
\name{resolve_cobra_identity_code}
\alias{resolve_cobra_identity_code}
\title{Resolve a cobra identity name to abr's numeric card code}
\usage{
resolve_cobra_identity_code(names, cardpool_titles)
}
\arguments{
\item{names}{Character vector of cobra identity names (may contain NA).}

\item{cardpool_titles}{A data frame with \code{code}/\code{title} columns.}
}
\value{
Character vector of resolved codes, \code{NA} where \code{names} is \code{NA}
or has no matching title.
}
\description{
The lookup is many-to-one: an identity title reprinted across sets has
several codes (e.g. "NBN: Making News" has three in the live cardpool
release). Ties break on the lexicographically smallest code, so the
result is deterministic across builds rather than dependent on input
row order. This is sound only as a \strong{faction key}, not a claim about
which printing was played: no identity title in the cardpool release
spans more than one \code{faction_code}, and the only readers of this
column (\code{views-meta-stats.R}, \code{views-meta-trends.R}) use it purely to
look up a faction. A future view that renders the winning card image
must resolve the printing itself rather than trust this code.
(DL-052, DL-065)
}
\keyword{internal}
