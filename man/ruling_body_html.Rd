% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rulings.R
\name{ruling_body_html}
\alias{ruling_body_html}
\title{Render one ruling's Markdown body as sanitised HTML}
\usage{
ruling_body_html(text)
}
\arguments{
\item{text}{Character. One ruling body.}
}
\value{
A shiny tag, or NULL for empty text.
}
\description{
NetrunnerDB ruling text is Markdown -- blockquotes for the answer,
inline links to other cards. Rendered rather than shown raw, because
raw would put a literal \verb{[Foo](https://...)} in front of a reader.
}
\details{
SANITISING IS DONE HERE, ON THE INPUT, because commonmark 2.0.0
removed its \code{sanitize} argument and passes embedded HTML through
untouched -- \code{markdown_html("<script>x</script>")} returns that script
tag intact. This is third-party text arriving over the network into a
page, so HTML tags are stripped from the source before rendering.

Stripping \verb{<...>} from the SOURCE rather than escaping it: escaping
would turn \verb{&gt;} into a literal and break the blockquotes
NetrunnerDB writes its answers in, which is most of the formatting
worth keeping. The cost is that Markdown autolinks (\verb{<https://...>})
are dropped too; NetrunnerDB writes links as \verb{[label](url)}, so
nothing in the mirrored corpus relies on them today.
}
\keyword{internal}
