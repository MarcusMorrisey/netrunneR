% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-cardpool.R
\name{select_cardpool_columns}
\alias{select_cardpool_columns}
\title{Select a table's allowlisted columns and report any dropped upstream keys}
\usage{
select_cardpool_columns(df, allowlist, table_name)
}
\arguments{
\item{df}{A tibble parsed from upstream JSON.}

\item{allowlist}{Character vector of columns to keep.}

\item{table_name}{Character. Used only in the unknown-key message.}
}
\value{
A list with \code{data} (the selected tibble, with every allowlisted
column present) and \code{unknown_keys} (character vector of upstream
column names not in \code{allowlist}).
}
\description{
Uses any_of() rather than ABR's all_of(), and backfills any allowlisted
column absent from every row of this batch with NA: unlike the
ABR_TOURNAMENT_ALLOWLIST case (fixed personal-data fields, always
present), a cardpool JSON record's own optional fields (a card with no
\code{cost}/\code{strength}, e.g. an identity) can be entirely absent from
jsonlite::fromJSON()'s flattened output rather than merely NA-valued,
and the schema (inst/sql/schema/cardpool.sql) declares these nullable,
not required.
}
\keyword{internal}
