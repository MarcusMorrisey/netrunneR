% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_card_detail.R
\name{mod_card_detail_ui}
\alias{mod_card_detail_ui}
\title{Shared component: image + stats + text for one card, invoked as a modal.}
\usage{
mod_card_detail_ui(id)
}
\arguments{
\item{id}{Module id.}
}
\description{
Exported because \code{inst/shiny-app/app.R} calls this from outside the
package namespace -- \verb{inst/} files are sourced by
\code{shiny::shinyAppDir()} separately from the package's own namespace, so
anything they call must be reachable via \verb{netrunneR::}, not a bare
name.
}
\details{
shiny is referenced with explicit \verb{shiny::} prefixes throughout this
file (rather than an \verb{@import shiny}), matching how every other
package in this codebase's Imports is used -- only rlang's \code{abort} and
\code{.data} are imported by name, in R/netrunneR-package.R.
}
