% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cobra-tournament-corrections.R
\name{read_cobra_tournament_date_corrections}
\alias{read_cobra_tournament_date_corrections}
\title{Known-bad cobra tournament date corrections}
\usage{
read_cobra_tournament_date_corrections(
  path = system.file("extdata", "cobra_tournament_date_corrections.csv", package =
    "netrunneR")
)
}
\arguments{
\item{path}{Character. Defaults to the packaged template.}
}
\value{
A tibble with one row per known-bad (tournament_id, corrected_date).
}
\description{
Cobra (NSG's own tournament platform) occasionally stores a garbage
\code{date} value for a tournament -- e.g. tournament_id 4910's \code{date}
field reads "1900-07-13" even though its own \code{created_at} is
2026-07-07T11:50:47.674Z, for a tournament titled "Netrunner PH
Online League July 2026". This is a data-quality bug in cobra's own
source data (most likely an organizer entering a date without a year,
with the platform defaulting to epoch/1900), not something ingestion
or the merge can detect algorithmically -- a wrong-but-plausible-
looking date wouldn't reliably distinguish itself from a real one.
Confirmed corrections are recorded here by \code{tournament_id}, mirroring
the human-verified override pattern already established in
\code{R/abr-cobra-xref.R}, and applied during \code{build_cobra()} immediately
after cobra's raw bundles are flattened into \code{tournament} -- before
either the merge (\code{merge_abr_cobra()}) or any other downstream table
reads \code{date}, so both cobra's own \code{tournament} table and the merged
\code{tournament_merged} feed see the corrected value.
}
\keyword{internal}
