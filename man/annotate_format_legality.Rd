% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legality.R
\name{annotate_format_legality}
\alias{annotate_format_legality}
\title{Annotate cards against a format snapshot}
\usage{
annotate_format_legality(
  cards,
  snapshot,
  card_pool_set,
  restriction_card,
  printing,
  card_set
)
}
\arguments{
\item{cards}{The cardpool \code{card} table.}

\item{snapshot}{A one-row snapshot from \code{\link[=active_snapshot]{active_snapshot()}}, or NULL
for "no format applies", which leaves every card in pool and clean.}

\item{card_pool_set}{The cardpool \code{card_pool_set} table.}

\item{restriction_card}{The cardpool \code{restriction_card} table.}

\item{printing}{The cardpool \code{printing} table.}

\item{card_set}{The cardpool \code{card_set} table.}
}
\value{
\code{cards} with six columns added.
}
\description{
The v2 counterpart to \code{\link[=annotate_legality]{annotate_legality()}}, and the one to prefer.
It differs in two ways that matter:
}
\details{
\itemize{
\item Card-pool membership is read POSITIVELY from the snapshot's pool
(the sets that are in) rather than inferred from a rotation's list
of cycles that rotated out.
\item The ban list is the one the snapshot names, so it is always the
right list for the format -- the legacy path had to guess a list's
format from its code prefix, which was wrong for 13 of 41 lists.
}

The first four columns added are named exactly as \code{\link[=annotate_legality]{annotate_legality()}}
names them, so \code{\link[=filter_legal]{filter_legal()}} and \code{\link[=legality_search_fields]{legality_search_fields()}} work
against either path's output. \code{in_rotation} here means "in this
snapshot's card pool".

Two further columns carry what the legacy tables could not express:
\code{global_penalty}, and \code{points} for the points-list formats (Eternal),
where a card is not banned but costs against a deck's point limit.
}
