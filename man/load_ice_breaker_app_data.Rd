% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{load_ice_breaker_app_data}
\alias{load_ice_breaker_app_data}
\title{Load the data the ice/breaker Shiny app needs, once per process}
\usage{
load_ice_breaker_app_data()
}
\value{
A list with \code{cards}, \code{legality} (the CARDPOOL_LEGALITY_TABLES,
each NULL when the release predates that schema), \code{matchup} and
\code{traits} (the \code{ice_breaker_traits} the matchup table was built
from), \code{tournaments} (the abr \code{tournament} table, NULL when no abr
release is active), \code{identities} and \code{factions} (the cardpool
identity cards and the faction lookup, which the meta stats view
needs and \code{cards} cannot supply), \code{cardpool_release_id} and
\code{implementation_release_id} (the release directory names
\code{compute_ice_breaker_matchups()} was called with -- already computed
for that call, surfaced here rather than recomputed, so a
long-running app can show which release it is serving without a
second call into the store), or \code{missing_lineages} (character
vector, non-NULL) if either required release is unavailable --
callers branch on \code{missing_lineages} before touching the rest.

\code{cards} remains \code{ice_breaker_pool(card)} and therefore still holds only
ice and icebreakers. Adds \code{all_codes}: a tibble of \code{code}, \code{title} and \code{type_code}
for EVERY card in the active cardpool release, projected from the \code{all_cards} object
already materialised here for the identities split.

\code{all_codes} exists because \code{cards} cannot answer "does this release know this code".
A deck's Agenda, Event and Operation codes are absent from \code{cards} for a reason that
has nothing to do with the release being stale, so asking \code{cards} merges "unknown to
this release" with "known but not ice or a breaker" - the exact merge Deck Compare's
unresolved-code state is required not to make. Ask \code{all_codes}, never \code{cards}. (DL-035)
}
\description{
Hoisted out of \code{app_server()} (called once from \code{inst/shiny-app/app.R},
not per session) so every browser session shares one computation and
one pair of SQLite reads rather than each session independently
reopening both databases and rerunning the ice x breaker cross-join --
cardpool/implementation data is static for the life of the R process.
}
