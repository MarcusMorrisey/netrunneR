% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_filter_bar.R
\name{apply_tournament_filters}
\alias{apply_tournament_filters}
\title{Apply every filter the bar carries}
\usage{
apply_tournament_filters(dated, filters, draft_codes = character(0))
}
\arguments{
\item{dated}{Output of with_parsed_dates().}

\item{filters}{A list of \code{dates}, \code{types} and \code{include_draft}, from
mod_filter_bar_server(). NULL applies nothing.}

\item{draft_codes}{Character vector from draft_identity_codes().}
}
\value{
The surviving rows, or NULL when there is nothing to filter.
}
\description{
ONE FUNCTION, so a view cannot apply two of the three and quietly skip
the third. Each filter is a no-op when its input is NULL, which is how
a release that predates a column, or a caller with no filter bar at
all, gets served rather than errored at.
}
\keyword{internal}
