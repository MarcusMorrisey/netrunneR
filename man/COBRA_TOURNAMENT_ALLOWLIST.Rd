% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build-cobra.R
\name{COBRA_TOURNAMENT_ALLOWLIST}
\alias{COBRA_TOURNAMENT_ALLOWLIST}
\title{Fail-closed dplyr::select() allowlists for the Cobra tables}
\usage{
COBRA_TOURNAMENT_ALLOWLIST
}
\description{
Cobra's pairings and standings endpoints carry real player display
names (player1_name/player2_name/name_with_pronouns) directly in the
response body -- unlike ABR, whose winner_*_identity fields name a
game identity, not a person. These allowlists exclude every such
field on the same precedent DEFAULT_DENY_PATTERN and
ABR_TOURNAMENT_ALLOWLIST already establish: personal data is excluded
by two independent fail-closed layers, never passed through and
relied on downstream code not to use. Layered with
check_deny_pattern(COBRA_DENY_PATTERN) below as an independent second
check, not a restatement of these allowlists. (ref: DL-002)
}
\details{
ABR_CODE DOES NOT REFERENCE abr.tournament.id -- CORRECTED 2026-09-05.
The prior version of this comment (DL-042/DL-043) asserted abr_code was
"a plain reference to abr.tournament.id in the abr lineage", inferred
from the field existing and being populated, never checked against
real values. It is wrong. The same real-world tournament mirrored in
both lineages ("Startup @ Entoyment. Community Tournament.",
2024-10-06) has cobra.tournament.abr_code = "667219" for the event
abr.tournament.id records as 4404 -- confirmed live against both
mirrors and against alwaysberunning.net's own API directly. ABR's own
tournament ids run 1-5842 (4433 tournaments, confirmed live); abr_code
values are six digits, outside that range for every populated row
inspected. What abr_code actually references upstream is
unconfirmed -- possibly a per-claim/report id rather than a
per-tournament one, since ABR tracks individual match claims at much
higher volume than tournaments, but that is a hypothesis, not
established here.

abr_code IS real, but not this. Cobra's public GitHub source
(Null-Signal-Games/cobra, app/services/abr_upload.rb) shows it POSTs
tournament results to ABR's own /api/nrtm upload endpoint, which
responds with a claim identifier -- the fixture at
spec/fixtures/vcr_cassettes/upload_to_abr.yml records a real example
response, {"code":301063}. That is what abr_code holds: ABR's claim
id for the upload, not abr.tournament.id. Confirmed live: querying
https://alwaysberunning.net/api/nrtm/<abr_code value> returns 401
(exists, needs ABR API auth we do not have), not 404 (does not
exist) -- the resource is real, just unreachable without
credentials neither netrunneR nor this mirror holds.

DO NOT use abr_code as a join key to abr.tournament.id. No public,
unauthenticated path from an abr_code value to any abr.tournament
row is known to exist. Bare column presence was mistaken for a
working key once already; the next attempt must verify actual
values (and, per this finding, actual auth requirements) before
designing a match tier around them, not after. (ref: DL-044)
}
\keyword{internal}
