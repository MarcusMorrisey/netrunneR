% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_lane_board.R
\name{mod_lane_board_server}
\alias{mod_lane_board_server}
\title{Lane board module server}
\usage{
mod_lane_board_server(
  id,
  cards,
  matchup,
  selected_code,
  on_add_ice = function() NULL,
  on_add_breaker = function() NULL
)
}
\arguments{
\item{id}{Module id.}

\item{cards}{The active cardpool's \code{card} data frame.}

\item{matchup}{The tibble \code{compute_ice_breaker_matchups()} returns.}

\item{selected_code}{The shared card-detail \code{reactiveVal} owned by
\code{app_server()}; this module only ever sets it.}

\item{on_add_ice}{Zero-argument function invoked when the operator asks
to add an ice. \code{app_server()} supplies the one that opens the search
modal, so this module does not need to know the modal exists.}

\item{on_add_breaker}{Zero-argument function, the breaker counterpart.}
}
\value{
A list of two functions, \code{add_ice(code)} and
\code{add_breaker(code)}, so the caller can push a card chosen elsewhere
into the board. The board owns its own contents; the search modal
only names a card.
}
\description{
Lane board module server
}
