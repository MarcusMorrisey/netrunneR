% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search-fields.R
\name{new_search_field}
\alias{new_search_field}
\title{Define one searchable field}
\usage{
new_search_field(column, type, aliases = character(0), split = NULL)
}
\arguments{
\item{column}{Character. Column name in the data being searched.}

\item{type}{One of "string", "integer", "date", "boolean", "array".
Determines which operators are accepted and how values are compared
(see the NetrunnerDB syntax reference: string fields do
case-insensitive substring matching and also accept \verb{/regex/};
integer and date fields accept \verb{< <= > >=}; boolean fields accept
true/false/t/f/1/0; array fields match individual elements exactly).}

\item{aliases}{Character vector of alternative names, e.g. \code{"t"} for
\code{card_type}.}

\item{split}{Character. For \code{type = "array"}, the delimiter that
splits a stored string into elements (the cardpool schema stores
subtypes as a single \code{" - "}-delimited \code{keywords} column rather than
a real array).}
}
\value{
A field spec list.
}
\description{
Define one searchable field
}
