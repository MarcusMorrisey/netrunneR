% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_filter_bar.R
\name{mod_filter_bar_server}
\alias{mod_filter_bar_server}
\title{Filter bar module server}
\usage{
mod_filter_bar_server(
  id,
  tournaments = NULL,
  rotation = NULL,
  identities = NULL
)
}
\arguments{
\item{id}{Module id.}

\item{tournaments}{The abr \code{tournament} table, or NULL. NULL renders
no bar at all rather than controls over an invented range.}

\item{rotation}{The cardpool \code{rotation} table, or NULL. NULL drops the
named-period shortcuts and leaves the slider, rather than inventing
rotation dates -- there are seven and they are not guessable.}

\item{identities}{The cardpool identity cards, used only to work out
which codes are draft identities. NULL drops the draft chip, because
without the faction table there is no way to know which they are and
a guess would silently drop real results.}
}
\value{
A reactive returning a list of \code{dates} (length-2 Date),
\code{group} ("all", "competitive" or "casual") and \code{types}
(character, empty meaning every type in that group).
}
\description{
Filter bar module server
}
