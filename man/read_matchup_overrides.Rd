% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{read_matchup_overrides}
\alias{read_matchup_overrides}
\title{Read the curated matchup overrides with declared column types}
\usage{
read_matchup_overrides(
  path = system.file("extdata", "matchup_overrides.csv", package = "netrunneR")
)
}
\arguments{
\item{path}{Character. Defaults to the packaged template.}
}
\value{
A tibble with one row per hand-verified (ice, breaker) pair.
}
\description{
Types are declared, never guessed. \code{inst/extdata/matchup_overrides.csv}
ships as a header-only template awaiting real hand-verified data, and
readr types every column of a zero-row file as character -- which made
\code{cost_to_break} a character column, so
\code{compute_ice_breaker_matchups()} aborted inside \code{dplyr::if_else()}
("Can't combine \code{true} \if{html}{\out{<character>}} and \code{false} \if{html}{\out{<integer>}}") the moment
it tried to fold an override into the integer cost column.
}
\details{
That crashed \code{load_ice_breaker_app_data()} for every caller, meaning
the app could not start against ANY real promoted release. It went
unnoticed because the matchup tests pass the fixture tribble from
helper-mini-pool.R, whose cost_to_break is already an integer;
nothing exercised this file until a test drove the app end to end.

Declaring types also fixes the subtler version of the same bug: with
rows present readr would guess integer and appear to work, so the
failure depended on whether anyone had populated the template yet.
}
\keyword{internal}
