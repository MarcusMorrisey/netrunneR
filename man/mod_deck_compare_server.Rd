% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mod_deck_compare.R
\name{mod_deck_compare_server}
\alias{mod_deck_compare_server}
\title{Deck Compare view module}
\usage{
mod_deck_compare_server(id, all_codes, cards, matchup, cardpool_release_id)
}
\arguments{
\item{id}{Module namespace id.}

\item{all_codes, cards, matchup, cardpool_release_id}{App data from
\code{\link[=load_ice_breaker_app_data]{load_ice_breaker_app_data()}}.}
}
\value{
\code{mod_deck_compare_ui()} returns a \code{shiny::tagList}; the server returns
invisibly and holds all deck state in session-scoped \code{reactiveVal()}s.
}
\description{
Deck Compare view module
}
\details{
A top-level UI mount exists here, unlike \code{mod_card_detail_ui()} and the
matchup explorer, because this is a nav destination swapped into the main slot rather
than a modal building its own outputs (DL-031). Nothing is persisted: no
\code{dbWriteTable()}, no \code{store_root}, no \code{.LINEAGE_REGISTRY} entry. Decks are discarded
on disconnect (DL-037). A refused fetch or refused deck renders through
\code{alert_box()} and leaves any prior result standing rather than blanking the view,
because Compare is an explicit press against free-text references and a typo should
not destroy the comparison being read (DL-040). Instantiate once per session; the
module is not re-instantiated on navigation.
}
