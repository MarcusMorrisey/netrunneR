% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{safe_render}
\alias{safe_render}
\title{Render a Shiny output's body, catching any error rather than letting it
surface as Shiny's default per-output red error banner (which is only
visible to someone looking at that browser tab at the moment it
happens).}
\usage{
safe_render(
  expr_fn,
  fallback_message = "Something went wrong displaying this."
)
}
\arguments{
\item{expr_fn}{A zero-argument function whose body may throw.}

\item{fallback_message}{Character shown in place of a thrown error.}
}
\description{
TODO: log the caught error into R/ledger.R's durable event log instead
of \code{message()} -- this was written without reading ledger.R's actual
append API, and \code{append_ledger()} is scoped to a specific lineage's
store_root, which a UI render error doesn't have, so wiring this in
properly needs a store_root-optional event sink, not just a direct
call. Left as a follow-up rather than guessed at here.
}
