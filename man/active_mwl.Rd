% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/legality.R
\name{active_mwl}
\alias{active_mwl}
\title{The most recent ban list for a format on a given date}
\usage{
active_mwl(mwl, format = "standard", as_of = Sys.Date())
}
\arguments{
\item{mwl}{The cardpool \code{mwl} table.}

\item{format}{Character. A real format id, now that \code{mwl.format} is
joined from the v2 restriction table rather than guessed from a code
prefix -- \code{"standard"} (the default), \code{"startup"}, \code{"eternal"} or
\code{"snapshot"}. There is no \code{"napd"} or \code{"sunset"} format; those
prefixes name Standard lists. Prefer \code{\link[=active_snapshot]{active_snapshot()}}, which also
resolves the card pool.}

\item{as_of}{Date. Defaults to today.}
}
\value{
A one-row data frame, or NULL when that format has no list yet
in force.
}
\description{
The most recent ban list for a format on a given date
}
