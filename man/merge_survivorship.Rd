% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge-abr-cobra.R
\name{merge_survivorship}
\alias{merge_survivorship}
\title{Fill a merged tournament row's columns from their preferred source}
\usage{
merge_survivorship(merged_id, abr_rows, cobra_rows)
}
\description{
abr wins title/date/format/type/location_* whenever an abr row
contributes -- cobra has no location column at all
(\code{COBRA_DENY_PATTERN} rejects lat/lon, and \code{cobra.sql} defines no
location field) and no curated \code{type} taxonomy equivalent to abr's.
\code{players_count} prefers cobra's stage-level figure (summed across a
group's cobra rows) when it is known and non-zero, else abr's scalar.
The two winner-identity columns prefer cobra's resolved code when one
exists, else abr's -- cobra's is recomputed from actual per-round
standings (\code{cobra_tournament_facts()}), abr's is a flat, uncheckable
scalar with no per-result data anywhere in that mirror to derive or
contradict it (DL-051, DL-066). \code{top_count} is abr-only; passthrough.
}
\keyword{internal}
