% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge-abr-cobra.R
\name{cobra_tournament_facts}
\alias{cobra_tournament_facts}
\title{Derive per-cobra-tournament facts that live outside \code{cobra.tournament}}
\usage{
cobra_tournament_facts(cobra_stage, cobra_standing)
}
\description{
\code{players_count} is \code{MAX(stage.player_count)} across a tournament's
stages (cobra tracks participation per stage, not on the tournament
row). Winner identity comes from the \code{position == 1} standing row,
preferring a complete elimination stage: \code{standing} carries no
\code{stage_id}, so it cannot be joined to \code{stage.is_elimination} --
\code{stage_format \%in\% c("double_elim", "single_elim")} is the schema's
own stand-in for that flag, confirmed against the live release to
cover exactly the same rows \code{stage.is_elimination = 1} does (DL-064).
A tournament can have more than one \code{position == 1} row across its
stages (417 such tournaments in the live release) -- this always
resolves to exactly one, never by input order: an elimination-stage
row with \code{rounds_complete > 0} if any exists, else the swiss-stage
row, and ties within that broken by ascending \code{player_id} for
determinism across builds.
}
\keyword{internal}
